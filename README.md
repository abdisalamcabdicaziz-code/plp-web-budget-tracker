# SpendWise - Interactive Dashboard (Week 6/7)

## Improvements & DOM Manipulation Implementation:
- Replaced any console logs and prompts with a fully interactive HTML form and event listeners.
- Integrated DOM manipulation (`document.createElement`, `innerHTML`, `textContent`) to dynamically render expense cards, totals, and alerts directly onto the screen without page reloads.
- Used loops and arrays (`expensesList`) to process data and conditional statements (`if/else`) to display dynamic status alerts based on the remaining budget.
